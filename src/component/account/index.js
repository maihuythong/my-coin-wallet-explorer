import React from 'react';
import Balance from '../balance';
import './style.scss';

const Account = (props) => {

  return (
    <div className="account">
      <Balance />
    </div>
  );
};

export default Account;
